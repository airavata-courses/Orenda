language: node_js
node_js: 10.16
before-install:
  - cd APIgateway
install:
  - npm install
script: 
#   - npm start
after_success:
  - docker login -u $DOCKER_USERNAME -p $DOCKER_PASSWORD
  - docker build -t ./APIGateway 
  - docker push michheta15/api_gateway

branches:
  only:
  - dockerizeApiGateway

